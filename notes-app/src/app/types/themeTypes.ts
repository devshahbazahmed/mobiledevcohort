export type themeTypes = {
  light: {
    background: string;
    card: string;
    text: string;
    placeholderText: string;
  };
  dark: {
    background: string;
    card: string;
    text: string;
    placeholderText: string;
    titleText: string;
  };
};
